{
  "name": "tiled-223d-world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test",
    "world-api": "node scripts/world-api-cli.mjs"
  },
  "bin": { "tiled-world-api": "scripts/world-api-cli.mjs" },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
