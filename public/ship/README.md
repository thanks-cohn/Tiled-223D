# Replace the sphere ship
Place your own model at `public/ship/ship.glb`. The viewer tries `/ship/ship.glb` once when it starts. If it is absent or invalid, a tiny procedural sphere is the ship. Keep the model origin near its center; target approximately two world units across. No GLB is required for the demo. Models are untrusted assets, not scripts.
